plugins {
  id "java-library"
}

ext {
  jdk_version = System.getenv('JAVA_VERSION')?.trim() ?: "$java_version"
}

subprojects {
  sourceCompatibility = "$jdk_version"
  targetCompatibility = "$jdk_version"

  repositories {
    jcenter()
    mavenCentral()
  }
}
